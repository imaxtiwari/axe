# Context Summary

## Task
Build deterministic, PII-safe error handling for AXE.

## Status
- Created `src/axe/exceptions.py` with `AXEError`, `AuthError`, `IsolationError`, `AuditError`.
- Registered exception handlers in `src/axe/main.py`.
- Updated `src/axe/security/isolation.py` to import `IsolationError` from `axe.exceptions`.
- Added tests in `tests/test_security.py`.
- Fixed `.to_response()` test to inspect `response.body` via `json.loads`.
- Per-class FastAPI exception handlers now catch AXE exceptions even with BaseHTTPMiddleware installed.
- Only remaining issue: catch-all `Exception` handler does not catch unknown exceptions (e.g. `RuntimeError`) when `BaseHTTPMiddleware` is active.

## Next Step
Add an outermost ASGI middleware fallback in `src/axe/exceptions.py` that catches any unhandled exception, converts it to the generic AXE 500 envelope, logs it, increments the counter, and install it in `src/axe/main.py` before all other middleware. Then re-run `python3 -m pytest tests/test_security.py -q`.

## Relevant Files
- src/axe/exceptions.py
- src/axe/main.py
- tests/test_security.py
- src/axe/security/context.py
- src/axe/observability.py
